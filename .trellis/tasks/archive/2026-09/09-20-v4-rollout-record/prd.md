# 记录 v4.0.0 上线结果并归档

## Goal

用户在自己机器上更新到 v4.0.0 并完成入口修改，要求归档本次上线结果。只做留痕：记录部署形态与改动结果、更新 journal，然后归档任务。不访问用户的机器，不做任何网络动作。

## Requirements

- 记录**形态**，不记录实例专有值：不写端口号、地址、主机名、凭据（全局红线第 3 条）。
- 只做留痕与归档；代码不动，门禁不需要重跑（tip 已由 CI 验证）。

## Acceptance Criteria

- [x] journal 有一条独立 session 记录上线结果
- [x] 任务 finish → archive
- [x] 记录里不含实例专有值

## 结果（2026-09-20）

用户在自己机器上完成 v4.0.0 更新，并按 README 的切换路径**启用 SOCKS5 入口 + 移除旧的
Shadowsocks-2022 入口**，切换成功。落地后的形态：

- 唯一可选入口 = 公网 **SOCKS5**（明文、握手可指纹识别，定位是救急的备用腿，不是主力）；
  安装流程仍然默认不建它，`[8]` 里可停用/改端口/改密码。
- 该机器上**不再有** `ss-sb` 入站：`ss.txt` 与客户端里的 ss 条目不再生成，
  修复/重写配置也不会再带它回来。
- Shadowsocks-2022 现在只剩**上游那一跳**（服务器↔服务器中转）在用；这是设计内的唯一保留用途。
- 代码里的 `preserved_*` 兼容路径**不删**：其它机器上可能还有 3.0.0–3.1.4 建的旧入口，
  而且旧入口一旦移除就**无法从菜单重建**（这是 4.0.0 的有意设计），所以保留路径必须留着。

## 复核

- 本轮无代码改动，不产生新的验证需求；发布 tip `5995e52` 已由 CI（run 35491370339）验证绿。
- 未联网、未访问用户的机器、未读取工作区外文件。
